export class InterestArea {
  constructor(
    public id: number,
    public area: string,
    public description: string,
    public interests: string, // TODO change to Interest
    public tasks: string) {
  }
}
